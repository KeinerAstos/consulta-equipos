services:
  - type: web
    name: consulta-equipos
    env: python

    buildCommand: |
      pip install -r requirements.txt

    startCommand: |
      gunicorn index:app --bind 0.0.0.0:$PORT --workers 1 --timeout 200
