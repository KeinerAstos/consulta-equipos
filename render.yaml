services:
  - type: web
    name: consulta-equipos
    env: python
    buildCommand: "bash setup.sh"
    startCommand: "gunicorn index:app --bind 0.0.0.0:$PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
