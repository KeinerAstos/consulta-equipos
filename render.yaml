services:
  - type: web
    name: consulta-equipos
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash install_chrome.sh && gunicorn index:app --bind 0.0.0.0:$PORT
    region: oregon
